# CS2 Analytics - Web App Environment Variables
# Copy this file to .env.local and adjust values as needed

# =============================================================================
# API CONFIGURATION
# =============================================================================

# Backend API URL
# Default: http://localhost:3000 (local development)
NEXT_PUBLIC_API_URL=http://localhost:3000

# =============================================================================
# FEATURE FLAGS
# =============================================================================

# Skip onboarding flow after login
# - "true": Users are redirected directly to dashboard after login
# - "false" or unset: Users go through onboarding if not completed
#
# Use this to temporarily disable onboarding while it's being developed
NEXT_PUBLIC_SKIP_ONBOARDING=false
